import { Typography, Stack } from '@mui/material';

const Home = () => {
  return (
    <Stack gap={3} mt={4} alignItems="center">
      <Typography variant="h1">Teste Fullstack Gazin</Typography>
      <Typography variant="h4">React + Mobx + MuI + ApiSauce + formik</Typography>
    </Stack>
  );
};

export default Home;
